<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:id="@+id/favorites"
  app:startDestination="@id/favoritesFragment">

  <fragment
    android:id="@+id/favoritesFragment"
    android:name="com.poul.android.pikabu.presentation.fragment.favorites_post.FavoritesFragment"
    android:label="Favorites"
    tools:layout="@layout/fragment_favorites">
  <action
      android:id="@+id/to_postFragment"
      app:destination="@id/postFragment"
      app:enterAnim="@anim/fragment_fade_enter"
      app:exitAnim="@anim/fragment_fade_exit"
      app:popEnterAnim="@anim/fragment_fade_enter"
      app:popExitAnim="@anim/fragment_fade_exit" />
  </fragment>

  <fragment
      android:id="@+id/postFragment"
      android:name="com.poul.android.pikabu.presentation.fragment.postdetail.PostFragment"
      android:label="One"
      tools:layout="@layout/fragment">
    <argument
        android:name="picture"
        app:argType="string" />
    <argument
        android:name="title"
        app:argType="string" />
    <argument
        android:name="body"
        app:argType="string" />
    <argument
        android:name="isFavorite"
        app:argType="boolean" />
  </fragment>
</navigation>